<div id="footer" class="bigBox" style="background:#1F1F1F;">
  @*<div class="box">

    @{
        int flag = 1;
        foreach (HelpInfo info in WorkContext.HelpList)
        {
            if (info.Pid != 0)
            {
                if (info.Url.Length == 0)
                {
            <a href="@Url.Action("question", "help", new RouteValueDictionary { { "id", info.Id } })">@info.Title</a>
                }
                else
                {
            <a href="@info.Url">@info.Title</a>
                }
            }
            else
            {
                if (flag != 1)
                {
                @:</div>
            }
            @:<div class="footerBox"><h3>@info.Title</h3>
            flag++;
            }
        }
        if (WorkContext.HelpList.Count > 0)
        {
        @:</div>
    }
    }

  <div class="clear"></div>
  </div>*@
  <div class="bigBox footerB">
    <div id="footerB">
      <div class="links">
        @for(int i = 0; i < WorkContext.FriendLinkList.Length;i++ )
        {
            FriendLinkInfo info = WorkContext.FriendLinkList[i] as FriendLinkInfo;
        <a href="@info.Url" target="@info.Target" title="@info.Title">@info.Name</a> 
            if(i!=WorkContext.FriendLinkList.Length-1){
                                                                                         @:|
          }
        }
      </div>
      <div><ul style="width:1000px;margin:0 auto; text-align: center;margin-top:7px;">
    <li>免责声明：。</li>
    <li>© 2017 保留所有权利</li>
    <li>备xxxxxxx号</li>
    </ul></div>
    </div>
  </div>
</div>
